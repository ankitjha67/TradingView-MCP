"""
The strategy library.

Each module holds one category. Every model is a distinct, published method with
a citation — not a parameter permutation of its neighbours. Models that can only
be approximated from single-series bar data set ``is_proxy = True`` and say so.
"""
